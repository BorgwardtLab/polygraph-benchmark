# H100 GPU SLURM configuration for faster TabPFN acceleration.
# Targets hpcl91 and hpcl94g nodes with H100 GPUs.
#
# Usage:
#   python XX/compute.py --multirun hydra/launcher=slurm_gpu_h100

defaults:
  - submitit_slurm

partition: p.hpcl91,p.hpcl94g
timeout_min: 1440
nodes: 1
tasks_per_node: 1
cpus_per_task: 8
gres: "gpu:h100:1"
exclude: "hpcl9101"
mem_gb: 32
setup:
  - "export TABPFN_ALLOW_CPU_LARGE_DATASET=1"
  - "echo CUDA_VISIBLE_DEVICES=$CUDA_VISIBLE_DEVICES"
  - "nvidia-smi -L 2>/dev/null || echo 'nvidia-smi not available'"
  - "python -c 'import torch; print(\"torch.cuda.is_available():\", torch.cuda.is_available()); print(\"CUDA device count:\", torch.cuda.device_count() if torch.cuda.is_available() else 0)' 2>&1 || true"
array_parallelism: 1024
